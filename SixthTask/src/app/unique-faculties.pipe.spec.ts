import { UniqueFacultiesPipe } from './unique-faculties.pipe';

describe('UniqueFacultiesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueFacultiesPipe();
    expect(pipe).toBeTruthy();
  });
});
